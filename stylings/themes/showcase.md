# Theme showcase

This paragraph contains **strong text**, *emphasis*, `inline_code()` and a
[useful link](https://example.com). Math also gets its own colour: $a^2+b^2$.

> A block quote uses the quieter part of the palette without disappearing.

- [x] Coordinated document and syntax colours
- [ ] More themes can use the same compact overlay format

| Theme part | Purpose | Status |
|:-----------|:--------|:------:|
| Heading | hierarchy | ready |
| Rows | legibility | ready |
| Code | syntax | ready |

```c
static int answer(const char *name)
{
    /* Highlighted by the matching libfyts theme. */
    return name != NULL ? 42 : 0;
}
```

---

