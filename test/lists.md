# Hanging indent in lists

- A bullet item long enough to wrap: each invocation opens the repository's durable state, runs the requested verb, and exits.
- Short bullet.
  - A nested bullet that is also long enough to wrap onto a second row, so its continuation hangs under its own text.
    - Third level, again long enough to wrap and show that every level adds exactly its marker width.

1. An ordered item long enough to wrap: interactive mode keeps that same process alive for a fullscreen terminal session.
2. Short item.

A paragraph ends that list.

9. Nine.
10. A two-digit marker is one column wider, so this long item's continuation hangs one column further in than a single-digit item.

- [ ] An open task long enough to wrap, whose continuation lines line up under the task text rather than the checkbox.
- [x] A done task.

> - A bullet inside a blockquote, long enough to wrap, keeps the quote bar and hangs under the item text.

1. A loose item long enough to wrap onto the next row, followed by a second paragraph.

   The second paragraph starts at the item's content column too.
